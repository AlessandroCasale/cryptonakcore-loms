# Environment
# Profilo consigliato:
#   - DEV (locale):    ENVIRONMENT=dev
#   - PAPER-SERVER:    ENVIRONMENT=paper
#   - LIVE (in futuro): ENVIRONMENT=live
ENVIRONMENT=dev

# Database & logs
# N.B. Ogni ambiente può usare un DB diverso:
#   - DEV (locale):      sqlite:///./loms_dev.db        (esempio)
#   - PAPER-SERVER:      sqlite:///./loms_paper.db      (esempio)
# Se non lo cambi, il default resta sqlite:///./loms.db
DATABASE_URL=sqlite:///./loms.db

# File JSONL dove vengono salvati i segnali bounce
# (default: BASE_DIR/data/bounce_signals.jsonl)
# Puoi differenziare anche questo per ambiente, se vuoi:
#   - DEV:        services/cryptonakcore/data/bounce_signals_dev.jsonl
#   - PAPER-SERVER: services/cryptonakcore/data/bounce_signals_paper.jsonl
AUDIT_LOG_PATH=services/cryptonakcore/data/bounce_signals.jsonl

# OMS core
OMS_ENABLED=true
# Modalità broker: per ora supportato solo "paper".
# "live" è riservato per il futuro.
BROKER_MODE=paper

# --- Risk limits (paper / semi-live) ---
# Limite massimo di posizioni aperte in totale (None = nessun limite)
MAX_OPEN_POSITIONS=5

# Limite massimo di posizioni aperte per singolo simbolo (None = nessun limite)
MAX_OPEN_POSITIONS_PER_SYMBOL=2

# Size massima per posizione in USDT (paper)
MAX_SIZE_PER_POSITION_USDT=10.0
